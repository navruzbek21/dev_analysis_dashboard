# Прод-зависимости, зафиксированные на версиях, с которыми проходит тестовый прогон.
# Обновление — осознанным коммитом (bump + прогон pytest + pip-audit в CI).
dash==4.4.1
dash-bootstrap-components==2.0.4
plotly==6.9.0
pandas==3.0.5
numpy==2.4.6
redis==8.1.0
cachetools==7.1.7
orjson==3.12.0
pyarrow==25.0.1
python-dotenv==1.2.3
scikit-learn==1.9.0
statsmodels==0.14.6
gunicorn==26.1.0; platform_system != "Windows"
waitress==3.0.2; platform_system == "Windows"
